#include <stdio.h>

/**
 * main - function call for entet point
 *
 * printf - function call to print to standard output
 *
 * Return: always returns 0 at the end of the function "main" execution
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
