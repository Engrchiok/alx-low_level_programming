/**
 * mul - function definition for multiplyingtwo integers
 * @a: first parameter for the mul function
 * @b: second parameter for the mul function
 * Return: Always returns int
 */

int mul(int a, int b)
{
	return (a * b);
}
